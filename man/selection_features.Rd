% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_features.R
\name{selection_features}
\alias{selection_features}
\title{Build the selection-model feature frame from records.}
\usage{
selection_features(records, topics, labels, include = character(0))
}
\arguments{
\item{records}{A tibble with \code{document_id}, \code{country}, and the
\verb{relevance_score_*} / \verb{class_score_*} / \code{kw_total} columns (whatever is
present; absent columns are treated as \code{0}).}

\item{topics}{Named topic vector naming the cosine columns (required). The
BIOGAIN set is \code{biogain_assessment_topics()} in \code{planscanR.biogain}.}

\item{labels}{Named classifier-label vector naming the classifier columns
(required). The BIOGAIN set is \code{biogain_classification_labels()} in
\code{planscanR.biogain}.}

\item{include}{Optional extra feature columns to append (off by default).
Recognised: \code{"country"}, \code{"native_type"}. These are country-specific and
will not transfer to an unseen portal — opt in only when training and
predicting on the same set of countries.}
}
\value{
A tibble of \code{document_id}, \code{country}, and the feature columns, with a
\code{"feature_names"} attribute.
}
\description{
Produces a tibble carrying the record keys (\code{document_id}, \code{country}) plus
one column per feature in \code{\link[=selection_feature_names]{selection_feature_names()}}. Missing or
non-finite numeric features are filled with \code{0} (an unscored / unclassified
record reads as "no signal"), so the frame is fully determined by the
records and the feature spec — the key property that keeps training and
prediction aligned.
}
\details{
The returned tibble carries a \code{"feature_names"} attribute listing the
predictor columns (everything except the two key columns), which the trainer
uses to assign recipe roles.
}
\examples{
\dontrun{
recs <- index_cache(country = "nl")
X <- selection_features(recs)
attr(X, "feature_names")
}
}
