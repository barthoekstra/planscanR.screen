% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_selection.R
\name{save_selection_model}
\alias{save_selection_model}
\alias{load_selection_model}
\title{Persist / restore a trained selection model.}
\usage{
save_selection_model(model, path)

load_selection_model(path)
}
\arguments{
\item{model}{A \code{planscanR_selection_model}.}

\item{path}{File path (conventionally \code{selection_model.rds} in the cache
root, alongside \code{reviews.csv}).}
}
\value{
\code{save_selection_model()} returns \code{path} invisibly;
\code{load_selection_model()} returns the model (or \code{NULL} if absent).
}
\description{
Thin wrappers over \code{\link[=saveRDS]{saveRDS()}} / \code{\link[=readRDS]{readRDS()}} with a class check, so the app
and the acquisition runbook can share one artifact.
}
