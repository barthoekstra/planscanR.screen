% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_selection.R
\name{selection_cv_metrics}
\alias{selection_cv_metrics}
\title{Out-of-fold metrics for a trained model at an arbitrary threshold.}
\usage{
selection_cv_metrics(model, threshold = NULL, by_country = FALSE)
}
\arguments{
\item{model}{A \code{planscanR_selection_model}.}

\item{threshold}{Probability cutoff (defaults to the model's own).}

\item{by_country}{If \code{TRUE}, return one row per country plus an \code{"all"} row
(a \code{country} column is prepended); otherwise a single overall row.}
}
\value{
A metrics tibble (one row, or one per country + \code{"all"} when
\code{by_country = TRUE}), or \code{NULL} if the model has no OOF data.
}
\description{
Recomputes precision/recall/F1 + confusion from the model's stored
out-of-fold predictions — no retraining — so a UI can sweep the decision
threshold cheaply.
}
