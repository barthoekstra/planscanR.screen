% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_learning_curve.R
\name{selection_learning_curve}
\alias{selection_learning_curve}
\title{Learning curve for the learned selection model.}
\usage{
selection_learning_curve(
  records,
  reviews,
  topics,
  labels,
  learner = selection_learner_logistic(),
  sizes = NULL,
  test_frac = 0.25,
  repeats = 10,
  eval_source = "random",
  threshold = 0.5,
  seed = NULL,
  by_country = FALSE
)
}
\arguments{
\item{records}{A scored + classified tibble (from \code{\link[planscanR:get_assessments]{planscanR::get_assessments()}},
\code{\link[planscanR:index_cache]{planscanR::index_cache()}}, or the review-app snapshot) carrying the
\code{\link[=selection_features]{selection_features()}} columns.}

\item{reviews}{The review-decision tibble (the app's \code{reviews.csv}), with
\code{document_id}, \code{country}, \code{decision}, \code{source}, \code{reviewed_at}.}

\item{topics, labels}{The topic and classifier-label vectors naming the
feature columns (required); see \code{\link[=selection_feature_names]{selection_feature_names()}}.}

\item{learner}{A \link{selection_learner}. Defaults to
\code{\link[=selection_learner_logistic]{selection_learner_logistic()}}.}

\item{sizes}{Optional integer vector of training-label counts to evaluate.
\code{NULL} builds a grid of ~10-12 increasing sizes from 30 up to the maximum
train-pool size (always including that maximum). Supplied sizes larger than
the train pool are dropped, but the pool maximum is always kept.}

\item{test_frac}{Fraction of the labelled data held out as the fixed test set
(per repeat).}

\item{repeats}{Number of repeated held-out resamples.}

\item{eval_source}{Restrict labels to this review \code{source} (default
\code{"random"} — the unbiased sample). \code{NULL} uses every keep/drop label.}

\item{threshold}{Probability cutoff for the keep decision when scoring.}

\item{seed}{Optional RNG seed for reproducible splits.}

\item{by_country}{If \code{TRUE}, also slice the held-out test set per country and
compute one set of metrics per country at every (size, repeat), plus an
\code{"all"} row that matches the default output. A \code{country} column is
prepended. The model is still trained corpus-wide — this only varies the
evaluation slice, so it shows where the shared model performs as the
training pool grows. Slices smaller than 1 record are dropped silently.}
}
\value{
A long tibble with one row per (size, repeat), columns in order:
\code{size}, \code{n_train_used}, \code{rep}, \code{n_test}, \code{precision}, \code{recall}, \code{f1}. When
\code{by_country = TRUE}, a leading \code{country} column is added and there is one
row per (country, size, repeat) plus an \code{"all"} row per (size, repeat).
}
\description{
Estimates how the model's held-out F1 / precision / recall improve as the
number of human keep/drop labels grows. For each repeat it makes one
stratified train/test split (the test set is FIXED across all training sizes
within that repeat, so the metric is comparable as the training pool grows),
then fits the learner on increasing stratified subsamples of the training pool
and scores the held-out test set. Repeating the whole thing \code{repeats} times and
averaging (see \code{\link[=learning_curve_summary]{learning_curve_summary()}}) smooths out the split noise and
shows where the curve flattens.
}
\examples{
\dontrun{
recs <- index_cache(country = "nl")
rev <- read.csv(file.path(cache_dir, "reviews.csv"), colClasses = "character")
curve <- selection_learning_curve(recs, rev, repeats = 10)
learning_curve_summary(curve)
}
}
\seealso{
\code{\link[=learning_curve_summary]{learning_curve_summary()}} to aggregate the curve.
}
