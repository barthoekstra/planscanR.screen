% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_zeroshot.R
\name{classify_text}
\alias{classify_text}
\title{Classify a character vector against candidate labels.}
\usage{
classify_text(model, x, labels, multi_label = FALSE)
}
\arguments{
\item{model}{A \code{planscanR_classifier} object.}

\item{x}{Character vector to classify.}

\item{labels}{Named character vector: names are stable slugs (used as
output column names), values are the natural-language hypothesis phrases
fed to the model.}

\item{multi_label}{Logical; passed through to the backend.}
}
\value{
Numeric matrix \verb{[length(x), length(labels)]}.
}
\description{
Returns a numeric matrix with one row per element of \code{x} and one column per
label (columns named by the label slugs, i.e. \code{names(labels)}). Values are
label probabilities. With \code{multi_label = FALSE} each row sums to ~1
(softmax over labels); with \code{multi_label = TRUE} each cell is an
independent probability.
}
