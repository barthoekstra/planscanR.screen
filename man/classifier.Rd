% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_zeroshot.R
\name{classifier}
\alias{classifier}
\title{Build a custom planscanR classifier.}
\usage{
classifier(name, classify_fn)
}
\arguments{
\item{name}{Character scalar; recorded in the sidecar so a stored
classification knows which model produced it.}

\item{classify_fn}{A function \verb{function(x, labels, multi_label)} where \code{x}
is a character vector and \code{labels} is a named character vector
(names = slugs, values = hypothesis phrases). Must return a numeric
matrix with \code{length(x)} rows and one column per label, columns named by
the label slugs (\code{names(labels)}).}
}
\value{
An S3 object of class
\code{c("planscanR_classifier_custom", "planscanR_classifier")}.
}
\description{
Wraps a user-supplied \code{classify_fn} into an S3 object that participates in
the same interface as the built-in \code{\link[=classify_model_zeroshot]{classify_model_zeroshot()}}. Useful for
tests (a deterministic mock) or to plug in a different backend.
}
