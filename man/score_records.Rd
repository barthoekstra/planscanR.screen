% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relevance_model.R
\name{score_records}
\alias{score_records}
\title{Compute relevance scores for a set of records against one or more topics.}
\usage{
score_records(records, topic, model, text_fn = NULL)
}
\arguments{
\item{records}{A tibble; must include at least \code{country}, \code{title}, \code{summary}.}

\item{topic}{A character vector of topic phrases. Pass a named vector to
control the column-suffix slugs; unnamed elements get auto-slugified from
their phrase. See examples.}

\item{model}{A \code{planscanR_embedding_model} object.}

\item{text_fn}{Optional function \verb{function(record) -> character} that
builds the text to embed for each record. Default concatenates
\code{title} and \code{summary}.}
}
\value{
The input \code{records} tibble with one \verb{relevance_score_<slug>} column
per topic plus a single \code{relevance_model} column.
}
\description{
Cosine similarity between each topic's embedding and each record's title +
summary embedding. Records are embedded \strong{once} per call regardless of how
many topics are passed, so adding extra topics is essentially free.
}
\details{
Emits a one-shot warning if any record's country language falls outside
\code{supported_languages(model)}.
}
\examples{
\dontrun{
# Single topic — column will be relevance_score_wind_energy
score_records(recs, "wind energy", em)

# Multiple topics in one pass — column names will be relevance_score_wind etc.
score_records(
  recs,
  c(wind  = "wind energy",
    solar = "solar energy",
    res   = "regional energy transition strategy and planning"),
  em
)
}
}
