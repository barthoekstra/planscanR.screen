% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_selection.R
\name{consensus_reviews}
\alias{consensus_reviews}
\title{Resolve a review store to one agreed decision per record.}
\usage{
consensus_reviews(reviews, decisions = c("keep", "drop"))
}
\arguments{
\item{reviews}{The review-decision tibble (e.g. the app's \code{reviews.csv}),
with \code{document_id}, \code{country}, \code{decision}, \code{reviewer}, \code{reviewed_at}.}

\item{decisions}{Decisions treated as a verdict. Default \code{c("keep", "drop")}
(so \code{"unsure"} rows are ignored).}
}
\value{
A tibble with one row per agreed record: \code{document_id}, \code{country},
\code{decision}, \code{n_reviewers}, \code{reviewed_at} (the latest among the agreeing
reviewers). Empty if no record has an agreed decision.
}
\description{
Collapses the per-(record, reviewer) review rows into a single ground-truth
decision per \verb{(country, document_id)}, in two steps:
}
\details{
\enumerate{
\item \strong{Per reviewer:} keep each reviewer's \emph{most-recent} verdict on a record
(so re-deciding overrides an earlier opinion).
\item \strong{Across reviewers:} keep the record only if all its reviewers
\strong{unanimously agree}. Records with conflicting decisions are dropped as
ambiguous ground truth.
}

Records reviewed by a single reviewer trivially pass. This is the rule used
for both the training labels (\code{\link[=train_selection_model]{train_selection_model()}}) and the
automated-vs-human comparison, so a disagreement never silently becomes a
label.
}
\examples{
\dontrun{
rev <- read.csv("reviews.csv", colClasses = "character")
consensus_reviews(rev)
}
}
